[package]
name = "peregrine-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "colibrì engine core: config, safetensors index, quantized-tensor formats, dtype conversion"

[dependencies]
serde_json.workspace = true
peregrine-io = { path = "../peregrine-io" }
thiserror = "2"
parking_lot = "0.12"

# O_DIRECT flag is architecture-specific — don't hardcode it. Linux-only.
[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
