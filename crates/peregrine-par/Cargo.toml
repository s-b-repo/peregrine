[package]
name = "peregrine-par"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "peregrine: a tiny persistent scoped worker pool for bit-identical data-parallel compute (std-only)"
