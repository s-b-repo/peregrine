[package]
name = "peregrine-kernels"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "colibrì CPU integer-dot matmul kernels (scalar reference + AVX2/AVX-VNNI), token-exact"

[dependencies]
