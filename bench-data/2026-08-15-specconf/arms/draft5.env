# Baseline arm: MTP drafting at fixed depth 5, no confidence floor.
# Shared workload knobs mirror the 2026-08-13 defaults A/B so the datasets
# read against each other.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
export COLI_DRAFT=5
