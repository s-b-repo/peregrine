# Baseline arm: the curated fixed 8 GiB expert-cache budget
# (scripts/throughput-env.sh's value, measured basis recorded there).
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
