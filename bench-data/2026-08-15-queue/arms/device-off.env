# Baseline arm: device-blind claim cursor (the shipped default). Re-measures
# the 0.86 GB/s delivered-bandwidth figure (previously one rep) as this A/B's
# off side. Shared workload knobs mirror the 2026-08-15 arms so the datasets
# read against each other.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
