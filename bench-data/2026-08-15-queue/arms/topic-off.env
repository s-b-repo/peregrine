# Baseline arm: topic routing off (shipped default). The evictor breaks
# protection ties by global heat (nil on this CPU-only box). Shared workload
# knobs mirror the other 2026-08-15 arms.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
