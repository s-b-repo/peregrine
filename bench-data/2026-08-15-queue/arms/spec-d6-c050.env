# Sweep arm: COLI_DRAFT=6, confidence floor 0.5. Base knobs mirror the
# 2026-08-13/15 arm trios so every dataset reads against the others; the
# d5-c065 arm reproduces stage 4's candidate as the cross-run anchor.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
export COLI_DRAFT=6
export COLI_SPEC_CONF=0.5
