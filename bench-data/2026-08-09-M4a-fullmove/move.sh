#!/usr/bin/env bash
# MDIR anchor for swap-shard-links.sh; the move itself is done by the
# full-move runner (rsync + swap-shard-links --execute).
MDIR='/home/cortix/models/GLM-5.2-colibri-int4-with-int8-mtp'
