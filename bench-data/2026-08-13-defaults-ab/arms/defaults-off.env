# The pre-2026-08-13 behavior: every flipped default forced back to its
# historical off state. Shared workload knobs identical to defaults-on.env —
# the three lines below are the only difference between the arms.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
export COLI_FUSE_PREFILL=0
export COLI_PREFILL_CHUNK_DIV=0
export COLI_PREFIX_CACHE_MB=0
