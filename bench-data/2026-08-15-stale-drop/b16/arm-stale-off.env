# Baseline arm: prefetch stale-drop gate off (the shipped default).
# Shared workload knobs mirror the 2026-08-13 defaults A/B and the 2026-08-15
# overnight arms so the datasets read against each other.
export COLI_ECACHE_GB=8
export COLI_IO_BATCH=8
export COLI_KV_BUDGET_MB=4096
